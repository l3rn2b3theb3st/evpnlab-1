---

  - name: SHOW COMMAND ON DEVICES
    hosts: all
    # connection: network_cli
    gather_facts: no

    tasks:

      - name: SEND A SINGLE COMMAND
        nxos_command:
          commands: show system uptime
        register: output

      - name: VIEW FIRST ELEMENT OF A LIST
        debug:
          var: output['stdout'][0]

      # - name: SEND LIST OF COMMANDS
      #   nxos_command:
      #     commands:
      #       # - show version
      #       - show ip interface brief
      #   register: output

      # - name: VIEW SECOND ELEMENT OF A LIST
      #   debug:
      #     var: output['stdout'][1]